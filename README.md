

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GROQ_API_KEY ` in [.env.local](.env.local) and paste the `CESIUM_ION_TOKEN` from https://ion.cesium.com/ in [constants.ts].
3. Run the app:
   `npm run dev`
